# gas/ — Apps Script のスナップショット

**このディレクトリは実物ではなく、ある時点のコピーである。ここのコードを読んで「現行はこうなっている」と判断してはいけない。**

Apps Script の実体は各スプレッドシートに紐づくクラウド上のプロジェクトであり、このリポジトリはそこから `clasp` で取得したスナップショットを置いているだけである。誰かがスプレッドシートのエディタから直接編集すると、リポジトリは何も変わらないまま実体だけが進む。

## なぜ乖離が放置されるのか

Go には `go-lint.yml` と go-test があり、Flutter には analyze がある。壊れたコードや古いコードはCIが落ちて気づける。しかし **GAS を見るワークフローは存在しない**。`gas/` の中身が実体と何ヶ月ずれていても、テストもコンパイラも何も言わない。

実際に事故が起きている。2026-08-27、`gas/task/（内田）SeeFT送信.js` を現行コードと誤認して issue #466 と PR #467 を作ったが、そのファイルはライブに存在せず、記載した問題も存在しなかった。PR は close、issue は全面改訂になった。

## 作業前に必ずやること

GAS を触る作業は、リポジトリを読むことからではなく **実体を取得すること** から始める。

`clasp clone` は空ディレクトリでしか実行できないため、リポジトリ外の一時ディレクトリで取得する。以下はリポジトリルートで実行する。

```bash
npm --prefix gas install
repo_root=$(pwd); workdir=$(mktemp -d); echo "$workdir"
(cd "$workdir" && "$repo_root/gas/node_modules/.bin/clasp" clone <スクリプトID>)
```

取得したものとリポジトリを `diff` して、乖離の有無を先に把握してから方針を立てる。

```bash
diff -r "$workdir" gas/shift --exclude=.clasp.json
```

スクリプトIDは、対象スプレッドシートの `拡張機能 > Apps Script` を開いたときのURLから取る。`.clasp.json` はスクリプトIDを含むため `.gitignore` で除外している（本リポジトリは public のため）。

`clasp list` はログイン中アカウントが所有するものしか返さない。他人が所有するスクリプトは一覧に出ないが、編集権限とスクリプトIDがあれば `clone` / `pull` できる。

## ディレクトリと実体の対応

| ディレクトリ | 実体 | 最終同期 | 状態 |
| --- | --- | --- | --- |
| `shift/` | `45th_シフト_ver0` のプロジェクト（名簿・タスク・シフトの送信、人数チェック、条件付き書式、マニュアルURLの調査） | 2026-08-31 | 45th の現行。この日時点でライブと全8ファイル一致 |
| `rescue/` | レスキュー用スプレッドシートのプロジェクト（`doPost` のウェブアプリ、`onChange` でDB更新） | 未確認 | 45th で稼働中。同期状態は未検証 |
| `user/` | 未確認 | 未確認 | どのスプレッドシートに紐づくか未特定 |
| `task/` | 44th のタスクファイルに紐づく別プロジェクトの遺物 | — | **45th では使われていない。** `タスク塗り分け.js` が `44thTask_準備日` を参照していることから確認できる。ファイル名の `（内田）` は所有者の違いを示す |

45th のタスク送信は `task/` ではなく `shift/名簿タスク送信.js` が行っている。名前から推測すると間違える。

## 反映する

エディタへのコピペは不要で、`clasp push` で反映できる。

**編集も push も、clone した一時ディレクトリ（`$workdir`）で行う。** `gas/shift/` から push はできない。`clasp` はカレントディレクトリの `.clasp.json` で対象スクリプトを判断するが、そのファイルはスクリプトIDを含むため `.gitignore` で除外されており、リポジトリ側には存在しないためである。

GAS は全ファイルが同一スコープで評価されるため、`const` の重複定義があるとスクリプト全体が読み込み不能になり、スプレッドシートのメニューごと消える。push 前に必ず構文検査する。

```bash
cd "$workdir"
cat *.js > /tmp/all.js && node --check /tmp/all.js
"$repo_root/gas/node_modules/.bin/clasp" push -f
```

push が通ったら、同じファイルをリポジトリへ写して commit する。これが「同期」にあたる。

```bash
cp -f "$workdir"/*.js "$workdir"/appsscript.json "$repo_root/gas/shift/"
```

## 同期のタイミング

ライブを変更したら、同じ作業の中でリポジトリにも取り込む。「あとでまとめて」は必ず忘れる。実際、PR #469 でライブを取り込んだ直後にライブが先に進み、数日で再び乖離した。

取り込みのコミットは実体の丸写しにとどめ、コメントの修正やリファクタは混ぜない。混ぜるとその瞬間からリポジトリが実体と違うものになり、次に読む人が同じ罠を踏む。直したい箇所があるなら、取り込みとは別のコミットで直した上で `clasp push` して実体にも反映する。
